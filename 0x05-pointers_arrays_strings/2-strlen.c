#include "main.h"

/**
 * _strlen - returns the length of a string
 *
 * @s: sholds the string
 *
 * Return: Integer
 */

int _strlen(char *s)
{
	int length;

	length = strlen(*s);
	return (length);
}
